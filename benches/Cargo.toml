[package]
name = "benches"
version = "0.0.0"
edition.workspace = true
rust-version.workspace = true
publish = false

[dev-dependencies]
shiguredo_flac.workspace = true

# 計測 (ベンチマーク)
criterion = "0.8"

[[bench]]
name = "codec"
harness = false
