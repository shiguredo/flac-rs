[package]
name = "flac_decode"
version = "0.0.0"
edition.workspace = true
rust-version.workspace = true
description = "FLAC ファイルを WAV ファイルにデコードするサンプル"
publish = false

[dependencies]
shiguredo_flac.workspace = true

# 引数のパース
noargs = "0.3"
