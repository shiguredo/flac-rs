[package]
name = "flac_encode"
version = "0.0.0"
edition.workspace = true
rust-version.workspace = true
description = "WAV ファイルを FLAC ファイルにエンコードするサンプル"
publish = false

[dependencies]
shiguredo_flac.workspace = true

# 引数のパース
noargs = "0.3"
