# flac-rs

本リポジトリ固有の指示。

- **良い設計のためには破壊的変更を積極的に行うこと**
- 性能 (速度・圧縮率) より正しさ・堅牢性・シンプルさを最優先すること
  - SIMD は safe Rust を自動ベクトル化される形に整えることでのみ行うこと (arm64 / x86_64)
    - intrinsics (`core::arch`)・`unsafe`・nightly 限定機能 (`core::simd`) は使わないこと
    - 形を変えた最適化は変更前後でエンコード出力のバイト列が一致することを確認すること
  - サンプル値の算術は `i64` で行い、整数オーバーフローの可能性を排除すること (RFC 9639 Appendix A)
    - 格納幅だけを `i32` に狭めて演算前に widening する形は、結果が `i64` 算術とビット単位で一致するため使ってよい
    - 中間値まで狭幅で計算する場合は、溢れないビット深度の上限を定数として宣言し、呼び出し側でその範囲に限定すること (例: `fixed::BEST_ORDER_I32_MAX_BITS`)
  - 速度改善を試して効果がなかった、または逆効果だった手は `docs/failed-optimizations.md` に記録すること
    - 速度改善に着手する前に同ファイルを確認し、記録済みの手へ再挑戦しないこと
- Ogg / Matroska / MP4 などのコンテナマッピング (RFC 9639 Section 10) はスコープ外とすること

## RFC について

- RFC 9639 (FLAC) 準拠を最優先すること
- RFC を確認する際は refs/ 以下を利用すること
- ロスレス性を壊す変更は絶対にしないこと
  - エンコード → デコードで元のサンプル列と完全一致しなければならない

## サンプルについて

- サンプルは **お手本** なので性能と堅牢性を両立させること

## 本家実装との比較について

- 本家 flac コマンドとの比較は `make compare` (tools/flac_compare) で行うこと
  - 相互運用 (双方向の PCM 一致 + flac -t)・圧縮率 (メタデータ除外のフレームデータのみ)・速度 (CLI end-to-end) を一括で確認する
  - 本家 flac は環境変数 `FLAC_BIN`、次いで PATH から探す
- 性能 (速度・圧縮率) の変更を入れたときは `make compare` で本家比を確認すること

## 開発フローについて

- PR は作成しないこと
- feature ブランチを push し、CI が通ったら develop に直接マージすること
